/**
 * Constants for API response messages.
 * This module defines a collection of constants that are used throughout the application
 * to standardize the API response messages for various scenarios.
 */

const API_RESPONSES = {
    // Message returned when a user is not found in the database.
    USER_NOT_FOUND: "User not found!"
};

module.exports = { API_RESPONSES }
